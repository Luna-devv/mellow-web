"use client";

export default function Home() {

    // if (undefined) return (
    //     <div>
    //         {error && <ErrorBanner message={error} />}
    //     </div>
    // );

    return (
        <div>
            idk how to design this
        </div>
    );
}